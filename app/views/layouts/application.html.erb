<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    # chart.jsの導入
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.3/Chart.min.js"></script>

    <script src="https://kit.fontawesome.com/ecc368403e.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column vh-100">
    <header class="bg-dark shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a class="navbar-brand text-white" <%= link_to "Bookers", root_path %></a>
          <!-- ハンバーガーメニュー用 -->
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <%= render 'layouts/header' %>
      
        </div>
      </nav>
    </header>
    
    <%= flash[:notice] %>
    
    <!-- サーチ機能を表示したくないページに記載、NoMethodErrorが出るときに読み込む -->
    <% if ! @no_layouts_search.present? %>
      <div class="d-flex justify-content-center mb-2">
        <%= render 'searches/users_books_search' %>
        <%= render 'tag_searches/tag_search' %>
      </div>
    <% end %>
  
    <main class="mb-auto">
    <%= yield %>
    </main>

    <%= render 'layouts/footer' %>

  </body>
</html>
